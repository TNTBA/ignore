--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/usr/bin/debconf-get-selections	2021-10-01 05:39:27.000000000 -0400
+++ /media/mks/armbi_root/usr/bin/debconf-get-selections	2023-01-08 16:50:51.000000000 -0500
@@ -53,8 +53,12 @@
 }
 
 my $qi = Debconf::Question->iterator;
+my @qs;
 
 while (my $q = $qi->iterate) {
+	push @qs, $q;
+}
+for my $q (sort { $a->name cmp $b->name } @qs) {
 	my ($name, $type, $value) = ($q->name, $q->type, $q->value);
 	next if (! length $type || $type eq 'text' || $type eq 'title');
 	print "# ".$q->description."\n";
