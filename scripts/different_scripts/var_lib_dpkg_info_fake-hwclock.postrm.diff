--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/fake-hwclock.postrm	2016-04-15 10:50:12.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/fake-hwclock.postrm	2022-10-15 06:03:12.000000000 -0400
@@ -9,29 +9,18 @@
   *) :;;
 esac
 
-# Automatically added by dh_installinit
-if [ "$1" = "purge" ] ; then
-	update-rc.d fake-hwclock remove >/dev/null
+# Automatically added by dh_installinit/13.10
+if [ "$1" = "remove" ] && [ -x "/etc/init.d/fake-hwclock" ] ; then
+	chmod -x "/etc/init.d/fake-hwclock" >/dev/null || true
 fi
-
-
-# In case this system is running systemd, we make systemd reload the unit files
-# to pick up changes.
-if [ -d /run/systemd/system ] ; then
-	systemctl --system daemon-reload >/dev/null || true
+if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
+	update-rc.d fake-hwclock remove >/dev/null
 fi
 # End automatically added section
-# Automatically added by dh_systemd_enable
-if [ "$1" = "remove" ]; then
-	if [ -x "/usr/bin/deb-systemd-helper" ]; then
-		deb-systemd-helper mask fake-hwclock.service >/dev/null
-	fi
-fi
-
+# Automatically added by dh_systemd_enable/13.10
 if [ "$1" = "purge" ]; then
 	if [ -x "/usr/bin/deb-systemd-helper" ]; then
-		deb-systemd-helper purge fake-hwclock.service >/dev/null
-		deb-systemd-helper unmask fake-hwclock.service >/dev/null
+		deb-systemd-helper purge 'fake-hwclock.service' >/dev/null || true
 	fi
 fi
 # End automatically added section
