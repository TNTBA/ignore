--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/usr/share/initramfs-tools/hooks/klibc-utils	2021-06-05 13:14:49.000000000 -0400
+++ /media/mks/armbi_root/usr/share/initramfs-tools/hooks/klibc-utils	2021-12-26 12:51:17.000000000 -0500
@@ -19,10 +19,18 @@
 	;;
 esac
 
-# klibc
-cp -pnL /usr/lib/klibc/bin/* "${DESTDIR}/bin"
+for src in /usr/lib/klibc/bin/*; do
+	command="$(basename "$src")"
+	case "$command" in
+	gzip | kinit | zcat)
+		# Don't install; these will not be needed
+		;;
+	*)
+		# Don't install commands that already exist in /bin or /sbin
+		if ! [ -e "${DESTDIR}/sbin/$command" ]; then
+			cp -pnL "$src" "${DESTDIR}/bin"
+		fi
+		;;
+	esac
+done
 cp -pL /lib/klibc-*.so "${DESTDIR}/lib"
-rm -f "${DESTDIR}"/bin/kinit*
-if [ "${BUSYBOX}" = "n" ] || [ -z "${BUSYBOXDIR}" ]; then
-	rm -f "${DESTDIR}/bin/zcat"
-fi
