--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/nginx-common.preinst	2021-05-28 04:43:00.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/nginx-common.preinst	2023-03-14 11:19:32.000000000 -0400
@@ -1,20 +1,6 @@
 #!/bin/sh
 set -e
 
-# Handle naxsi removal
-dpkg-maintscript-helper rm_conffile \
-	  /etc/nginx/naxsi.rules         1.6.2-2~ -- "$@"
-dpkg-maintscript-helper rm_conffile \
-	  /etc/nginx/naxsi_core.rules    1.6.2-2~ -- "$@"
-dpkg-maintscript-helper rm_conffile \
-	  /etc/nginx/naxsi-ui.conf.1.4.1 1.6.2-2~ -- "$@"
-dpkg-maintscript-helper rm_conffile \
-	  /etc/nginx/naxsi-ui.conf       1.6.2-2~ -- "$@"
-
-# Handle upstart removal
-dpkg-maintscript-helper rm_conffile \
-	  /etc/init/nginx.conf           1.13.5-1~ -- "$@"
-
 case "$1" in
   install)
     # If we are doing a fresh install, then these files are no longer needed.
@@ -46,6 +32,11 @@
     ;;
 esac
 
+# Automatically added by dh_installinit/13.11.4
+if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/nginx" ] ; then
+	chmod +x "/etc/init.d/nginx" >/dev/null || true
+fi
+# End automatically added section
 
 
 exit 0
