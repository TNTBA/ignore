--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/x11-common.postinst	2017-05-03 05:38:16.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/x11-common.postinst	2021-08-18 07:00:51.000000000 -0400
@@ -1,4 +1,4 @@
-#!/bin/bash
+#!/bin/sh
 set -e
 
 if [ -f /usr/share/debconf/confmodule ]; then
@@ -7,16 +7,11 @@
   db_purge
 fi
 
-# Automatically added by dh_installinit
-if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
-	if [ -x "/etc/init.d/x11-common" ]; then
-		update-rc.d x11-common start 70 S . >/dev/null
-		if [ -n "$2" ]; then
-			_dh_action=restart
-		else
-			_dh_action=start
-		fi
-		invoke-rc.d x11-common $_dh_action || exit $?
+# Automatically added by dh_installinit/13.4+nmu1
+if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
+	if [ -z "${DPKG_ROOT:-}" ] && [ -x "/etc/init.d/x11-common" ]; then
+		update-rc.d x11-common defaults >/dev/null
+		invoke-rc.d --skip-systemd-native x11-common restart || exit 1
 	fi
 fi
 # End automatically added section
