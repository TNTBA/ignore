--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/usr/bin/sensible-browser	2018-03-12 06:17:53.000000000 -0400
+++ /media/mks/armbi_root/usr/bin/sensible-browser	2023-01-14 11:28:42.000000000 -0500
@@ -1,15 +1,24 @@
 #!/bin/sh
 
 # Prevent recursive loops, where these values are set to this script
-p="$(which sensible-browser)"
-[ "$(which $BROWSER || true)" = "$p" ] && BROWSER=
+p="$(command -v "$0")"
+[ -n "$BROWSER" ] && [ "$(command -v "$BROWSER" || true)" = "$p" ] && BROWSER=
+
+IsError()
+{
+	# Operating system command not found
+	[ "$1" -ne 126 ] && [ $1 -ne 127 ]
+}
+
+Run()
+{
+	"$@"
+	ret=$?
+	IsError "$ret"
+}
 
 if test -n "$BROWSER"; then
-    ${BROWSER} "$@"
-    ret="$?"
-    if [ "$ret" -ne 126 ] && [ "$ret" -ne 127 ]; then
-	exit "$ret"
-    fi
+    Run "${BROWSER}" "$@" && exit "$ret"
 fi
 
 if test -n "$DISPLAY"; then
@@ -31,6 +40,6 @@
     exec /usr/bin/www-browser "$@"
 fi
 
-printf "Couldn't find a suitable web browser!\n" >&2
-printf "Set the BROWSER environment variable to your desired browser.\n" >&2
+echo "Couldn't find a suitable web browser!\n" >&2
+echo "Set the BROWSER environment variable to your desired browser.\n" >&2
 exit 1;
