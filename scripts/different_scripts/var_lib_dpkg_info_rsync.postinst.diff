--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/rsync.postinst	2019-03-15 06:25:01.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/rsync.postinst	2022-12-18 09:10:54.000000000 -0500
@@ -1,31 +1,45 @@
-#!/bin/sh -e
-
-action="$1"
-
-if [ "$action" != configure ]; then
-    exit 0
+#!/bin/sh
+set -e
+# Automatically added by dh_installinit/13.11.3
+if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
+	if [ -z "${DPKG_ROOT:-}" ] && [ -x "/etc/init.d/rsync" ]; then
+		update-rc.d rsync defaults >/dev/null
+		if [ -n "$2" ]; then
+			_dh_action=restart
+		else
+			_dh_action=start
+		fi
+		invoke-rc.d --skip-systemd-native rsync $_dh_action || exit 1
+	fi
 fi
-
-
-if [ -x /etc/init.d/rsync ]; then
-    update-rc.d rsync defaults >/dev/null
-    invoke-rc.d rsync restart
+# End automatically added section
+# Automatically added by dh_installsystemd/13.11.3
+if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
+	if deb-systemd-helper debian-installed 'rsync.service'; then
+		# The following line should be removed in trixie or trixie+1
+		deb-systemd-helper unmask 'rsync.service' >/dev/null || true
+
+		if deb-systemd-helper --quiet was-enabled 'rsync.service'; then
+			# Create new symlinks, if any.
+			deb-systemd-helper enable 'rsync.service' >/dev/null || true
+		fi
+	fi
+
+	# Update the statefile to add new symlinks (if any), which need to be cleaned
+	# up on purge. Also remove old symlinks.
+	deb-systemd-helper update-state 'rsync.service' >/dev/null || true
 fi
-
-## from /usr/share/debhelper/autoscripts/postinst-systemd-enable :
-
-# This will only remove masks created by d-s-h on package removal.
-deb-systemd-helper unmask rsync.service >/dev/null || true
-
-# was-enabled defaults to true, so new installations run enable.
-if deb-systemd-helper --quiet was-enabled rsync.service; then
-	# Enables the unit on first installation, creates new
-	# symlinks on upgrades if the unit file has changed.
-	deb-systemd-helper enable rsync.service >/dev/null || true
-else
-	# Update the statefile to add new symlinks (if any), which need to be
-	# cleaned up on purge. Also remove old symlinks.
-	deb-systemd-helper update-state rsync.service >/dev/null || true
+# End automatically added section
+# Automatically added by dh_installsystemd/13.11.3
+if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
+	if [ -d /run/systemd/system ]; then
+		systemctl --system daemon-reload >/dev/null || true
+		if [ -n "$2" ]; then
+			_dh_action=restart
+		else
+			_dh_action=start
+		fi
+		deb-systemd-invoke $_dh_action 'rsync.service' >/dev/null || true
+	fi
 fi
-
-exit 0
+# End automatically added section
