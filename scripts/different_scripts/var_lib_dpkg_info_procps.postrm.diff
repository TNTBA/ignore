--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/procps.postrm	2018-05-31 05:42:46.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/procps.postrm	2022-12-19 01:06:38.000000000 -0500
@@ -44,20 +44,17 @@
 # dh_installdeb will replace this with shell code automatically
 # generated by other debhelper scripts.
 
-# Automatically added by dh_installinit/11.3.2
-if [ "$1" = "purge" ] ; then
-	update-rc.d procps remove >/dev/null
+# Automatically added by dh_installinit/13.11.3
+if [ "$1" = "remove" ] && [ -x "/etc/init.d/procps" ] ; then
+	chmod -x "/etc/init.d/procps" >/dev/null || true
 fi
-
-
-# In case this system is running systemd, we make systemd reload the unit files
-# to pick up changes.
-if [ -d /run/systemd/system ] ; then
-	systemctl --system daemon-reload >/dev/null || true
+if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
+	update-rc.d procps remove >/dev/null
 fi
 # End automatically added section
-# Automatically added by dh_installmenu/11.3.2
-if [ -x "`which update-menus 2>/dev/null`" ]; then update-menus ; fi
+# Automatically added by dh_installdeb/13.11.3
+dpkg-maintscript-helper rm_conffile /etc/sysctl.d/protect-links.conf 2:3.3.16-4\~ procps -- "$@"
+dpkg-maintscript-helper mv_conffile /usr/lib/sysctl.d/protect-links.conf /usr/lib/sysctl.d/99-protect-links.conf 2:3.3.17-6\~ procps -- "$@"
 # End automatically added section
 
 
