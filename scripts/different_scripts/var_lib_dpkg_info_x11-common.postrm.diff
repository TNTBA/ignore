--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/x11-common.postrm	2017-05-03 05:38:16.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/x11-common.postrm	2021-08-18 07:00:51.000000000 -0400
@@ -1,14 +1,7 @@
 #!/bin/sh
 set -e
-# Automatically added by dh_installinit
-if [ "$1" = "purge" ] ; then
+# Automatically added by dh_installinit/13.4+nmu1
+if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
 	update-rc.d x11-common remove >/dev/null
 fi
-
-
-# In case this system is running systemd, we make systemd reload the unit files
-# to pick up changes.
-if [ -d /run/systemd/system ] ; then
-	systemctl --system daemon-reload >/dev/null || true
-fi
 # End automatically added section
