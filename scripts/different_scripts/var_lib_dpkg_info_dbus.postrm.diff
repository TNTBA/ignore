--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/dbus.postrm	2020-07-05 12:10:45.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/dbus.postrm	2023-09-16 06:03:58.000000000 -0400
@@ -3,26 +3,29 @@
 set -e
 
 if [ "$1" = "purge" ] ; then
-	rmdir /var/run/dbus || true
-
-	rm -f /var/lib/dbus/machine-id
-	rmdir /var/lib/dbus || true
+	rmdir "${DPKG_ROOT:-/}var/run/dbus" || true
+	rmdir "${DPKG_ROOT:-/}var/lib/dbus" || true
 
 	LAUNCHER=/usr/lib/dbus-1.0/dbus-daemon-launch-helper
 
+	# This respects $DPKG_ROOT
 	if dpkg-statoverride --list "$LAUNCHER" >/dev/null 2>&1 ; then
 		dpkg-statoverride --remove "$LAUNCHER"
 	fi
 fi
 
-# Automatically added by dh_installinit/12.1.1
-if [ "$1" = "purge" ] ; then
+# Automatically added by dh_installinit/13.11.4
+if [ "$1" = "remove" ] && [ -x "/etc/init.d/dbus" ] ; then
+	chmod -x "/etc/init.d/dbus" >/dev/null || true
+fi
+if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
 	update-rc.d dbus remove >/dev/null
 fi
 # End automatically added section
-# Automatically added by dh_installdeb/12.1.1
-dpkg-maintscript-helper rm_conffile /etc/dbus-1/session.conf 1.10.0-4\~ dbus -- "$@"
-dpkg-maintscript-helper rm_conffile /etc/dbus-1/system.conf 1.10.0-4\~ dbus -- "$@"
+# Automatically added by dh_installsystemd/13.11.4
+if [ "$1" = remove ] && [ -d /run/systemd/system ] ; then
+	systemctl --system daemon-reload >/dev/null || true
+fi
 # End automatically added section
 
 
