--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/unattended-upgrades.preinst	2019-06-08 10:59:45.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/unattended-upgrades.preinst	2022-12-31 15:59:00.000000000 -0500
@@ -5,11 +5,15 @@
 # dh_installdeb will replace this with shell code automatically
 # generated by other debhelper scripts.
 
+# Automatically added by dh_installinit/13.11.4
+if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/unattended-upgrades" ] ; then
+	chmod +x "/etc/init.d/unattended-upgrades" >/dev/null || true
+fi
+# End automatically added section
 
 
 CONFIG="/etc/apt/apt.conf.d/50unattended-upgrades"
 
-dpkg-maintscript-helper rm_conffile $CONFIG 0.86.4~ -- "$@"
 # if the transition to ucf was not handled properly, do it now
 case "$1" in
     install|upgrade)
