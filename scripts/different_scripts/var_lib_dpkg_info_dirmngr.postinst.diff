--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/dirmngr.postinst	2022-07-01 12:06:43.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/dirmngr.postinst	2023-03-26 09:03:05.000000000 -0400
@@ -1,6 +1,25 @@
 #!/bin/sh
 set -e
-# Automatically added by dh_installdeb/12.1.1
+# Automatically added by dh_installsystemduser/13.11.4
+if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
+	if [ -z "${DPKG_ROOT:-}" ] ; then
+		# The following line should be removed in trixie or trixie+1
+		deb-systemd-helper --user unmask 'dirmngr.socket' >/dev/null || true
+
+		# was-enabled defaults to true, so new installations run enable.
+		if deb-systemd-helper --quiet --user was-enabled 'dirmngr.socket' ; then
+			# Enables the unit on first installation, creates new
+			# symlinks on upgrades if the unit file has changed.
+			deb-systemd-helper --user enable 'dirmngr.socket' >/dev/null || true
+		else
+			# Update the statefile to add new symlinks (if any), which need to be
+			# cleaned up on purge. Also remove old symlinks.
+			deb-systemd-helper --user update-state 'dirmngr.socket' >/dev/null || true
+		fi
+	fi
+fi
+# End automatically added section
+# Automatically added by dh_installdeb/13.11.4
 dpkg-maintscript-helper rm_conffile /etc/default/dirmngr -- "$@"
 dpkg-maintscript-helper rm_conffile /etc/dirmngr/dirmngr.conf -- "$@"
 dpkg-maintscript-helper rm_conffile /etc/dirmngr/ldapservers.conf -- "$@"
