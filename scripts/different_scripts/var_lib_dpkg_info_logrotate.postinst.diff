--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/logrotate.postinst	2018-08-28 18:21:11.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/logrotate.postinst	2022-12-14 13:16:50.000000000 -0500
@@ -1,8 +1,8 @@
 #!/bin/sh
 set -e
-# Automatically added by dh_installsystemd/11.3.5
+# Automatically added by dh_installsystemd/13.11.3
 if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
-	# This will only remove masks created by d-s-h on package removal.
+	# The following line should be removed in trixie or trixie+1
 	deb-systemd-helper unmask 'logrotate.timer' >/dev/null || true
 
 	# was-enabled defaults to true, so new installations run enable.
@@ -17,7 +17,7 @@
 	fi
 fi
 # End automatically added section
-# Automatically added by dh_installsystemd/11.3.5
+# Automatically added by dh_installsystemd/13.11.3
 if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
 	if [ -d /run/systemd/system ]; then
 		systemctl --system daemon-reload >/dev/null || true
