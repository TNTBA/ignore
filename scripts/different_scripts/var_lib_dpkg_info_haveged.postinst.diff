--- /media/mks/5dcae443-0796-4029-ae6c-bf5bd2a37067/var/lib/dpkg/info/haveged.postinst	2019-04-19 12:29:05.000000000 -0400
+++ /media/mks/armbi_root/var/lib/dpkg/info/haveged.postinst	2022-10-15 15:11:46.000000000 -0400
@@ -1,6 +1,6 @@
 #!/bin/sh
 set -e
-# Automatically added by dh_apparmor/2.13.2-10
+# Automatically added by dh_apparmor/3.0.7-1
 if [ "$1" = "configure" ]; then
     APP_PROFILE="/etc/apparmor.d/usr.sbin.haveged"
     if [ -f "$APP_PROFILE" ]; then
@@ -19,9 +19,22 @@
     fi
 fi
 # End automatically added section
-# Automatically added by dh_systemd_enable/12.1.1
+# Automatically added by dh_installinit/13.10
 if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
-	# This will only remove masks created by d-s-h on package removal.
+	if [ -z "${DPKG_ROOT:-}" ] && [ -x "/etc/init.d/haveged" ]; then
+		update-rc.d haveged defaults >/dev/null
+		if [ -n "$2" ]; then
+			_dh_action=restart
+		else
+			_dh_action=start
+		fi
+		invoke-rc.d --skip-systemd-native haveged $_dh_action || exit 1
+	fi
+fi
+# End automatically added section
+# Automatically added by dh_installsystemd/13.10
+if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
+	# The following line should be removed in trixie or trixie+1
 	deb-systemd-helper unmask 'haveged.service' >/dev/null || true
 
 	# was-enabled defaults to true, so new installations run enable.
@@ -36,11 +49,16 @@
 	fi
 fi
 # End automatically added section
-# Automatically added by dh_installinit/12.1.1
+# Automatically added by dh_installsystemd/13.10
 if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
-	if [ -x "/etc/init.d/haveged" ]; then
-		update-rc.d haveged defaults >/dev/null
-		invoke-rc.d haveged start || exit 1
+	if [ -d /run/systemd/system ]; then
+		systemctl --system daemon-reload >/dev/null || true
+		if [ -n "$2" ]; then
+			_dh_action=restart
+		else
+			_dh_action=start
+		fi
+		deb-systemd-invoke $_dh_action 'haveged.service' >/dev/null || true
 	fi
 fi
 # End automatically added section
